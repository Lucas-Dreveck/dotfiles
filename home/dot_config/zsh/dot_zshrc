ZSH_CONF_D="${ZDOTDIR:-${XDG_CONFIG_HOME:-$HOME/.config}/zsh}/conf.d"

if [[ -d "$ZSH_CONF_D" ]]; then
  for _conf_file in "$ZSH_CONF_D"/*.zsh(N) "$ZSH_CONF_D"/*/*.zsh(N); do
    source "$_conf_file"
  done
  unset _conf_file
fi
